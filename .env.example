# Server
NODE_ENV=development
PORT=3001

# Database (required in production; optional in dev if using defaults)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=spamsite
DB_USER=
DB_PASSWORD=

# CORS: comma-separated origins, or * to allow all
# Example: https://myapp.vercel.app,https://myapp.com
CORS_ALLOWED_ORIGINS=*
